tuple = (1, 2, 3, 2, 5, 2, 7, 8, 9)
# t[0]= 4    # We can't change the values of elements of tuple.
a = ()    # Empty tuple
print(a)
b = (1, ) # Tuple with only one elements , We'll have to use  a comma 
print(b)
c = ()
# Tuples Method
print(tuple.count(2)) # prints the no. of occurence of 2
print(tuple.index(2)) # prints the index of first occurence of 2
